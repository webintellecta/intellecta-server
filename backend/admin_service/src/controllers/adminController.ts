import { Request, Response } from "express";

// export const createCourse = async(req:Request, res: Response)=> {
//     const {title, subject, description, gradeLevel, difficultyLevel,thumbnail}=req.body;

// }

// export 


